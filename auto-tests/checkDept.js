const mongoose = require('mongoose'); require('dotenv').config(); mongoose.connect(process.env.MONGODB_URI).then(async () => { try { const departmentSchema = new mongoose.Schema({ name: String, code: String, head: { type: mongoose.Schema.Types.ObjectId, ref: 'User' } }); const Department = mongoose.model('Department', departmentSchema); const department = await Department.findById('68d1adfa494a90816679f116'); console.log('Department head:', department.head); } catch(err) { console.error(err); } finally { mongoose.connection.close(); } })
